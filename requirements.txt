# Core library (parsers, matching, LLM, DB) — shared across all UIs.
python-docx>=1.1
pypdf>=4.0
scikit-learn>=1.4
numpy>=1.26
google-genai>=1.0
python-dotenv>=1.0
python-jobspy>=1.1
pandas>=2.0

# v2 UI — FastAPI + HTMX + Tailwind (CDN) + DaisyUI (CDN)
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.12   # for file uploads

# streamlit>=1.32 removed 2026-08-03 — v1 UI archived at _deprecated/ui_streamlit/.
# If you need to run the archived UI: `pip install streamlit` then `streamlit run _deprecated/ui_streamlit/app.py`
